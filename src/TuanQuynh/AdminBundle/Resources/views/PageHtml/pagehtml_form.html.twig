{# {% form_theme form 'bootstrap_3_layout.html.twig' %} #}
{% form_theme form 'TuanQuynhAdminBundle:Form:form.html.twig' %}

<div class="row">
    <div class="col-lg-12">
        <div class="ibox float-e-margins">
            <div class="ibox-title">
                <h5>Change user profile</h5>
                <div class="ibox-tools">
                    <a class="collapse-link">
                        <i class="fa fa-chevron-up"></i>
                    </a>
                </div>
            </div>
            <div class="ibox-content">

                {{ form_errors(form) }}

                {{ form_start(form, {'class': 'fos_user_profile_edit form-horizontal'}) }}
                    {# {{ form(form) }} #}
                    {{ form_row(form.title) }}
                    {{ form_row(form.slug) }}
                    {{ form_row(form.content) }}
                    {{ form_row(form.isActive) }}
                    {{ form_widget(form.save) }}
                {{ form_end(form) }}
                {#
                <form action="{{ path('fos_user_profile_edit') }}" {{ form_enctype(form) }} method="POST" class="fos_user_profile_edit form-horizontal">
                {{ form_widget(form._token) }}
                    <div class="form-group {% if form.username.vars.errors is not empty %}has-error{% endif %}">
                        <label class="col-sm-2 control-label">{{ form_label(form.username) }}</label>

                        <div class="col-sm-10">
                            {{ form_widget(form.username, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
                            {% for errorItem in form.username.vars.errors %}
                              <label class="control-label has-error" for="{{ form.username.vars.id }}">{{ errorItem.message }}</label>
                            {% endfor %}
                        </div>
                    </div>
                    <div class="hr-line-dashed"></div>
                    <div class="form-group {% if form.email.vars.errors is not empty %}has-error{% endif %}">
                        <label class="col-sm-2 control-label">{{ form_label(form.email) }}</label>
                        <div class="col-sm-10">
                            {{ form_widget(form.email, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
                            {% for errorItem in form.email.vars.errors %}
                              <label class="control-label has-error" for="{{ form.email.vars.id }}">{{ errorItem.message }}</label>
                            {% endfor %}
                        </div>
                    </div>
                    <div class="hr-line-dashed"></div>
                    <div class="form-group {% if form.current_password.vars.errors is not empty %}has-error{% endif %}">
                        <label class="col-sm-2 control-label">{{ form_label(form.current_password) }}</label>
                        <div class="col-sm-10">
                            {{ form_widget(form.current_password, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
                            {% for errorItem in form.current_password.vars.errors %}
                              <label class="control-label has-error" for="{{ form.current_password.vars.id }}">{{ errorItem.message }}</label>
                            {% endfor %}
                        </div>
                    </div>
                    <div class="hr-line-dashed"></div>
                    <div class="form-group">
                        <div class="col-sm-4 col-sm-offset-2">
                            <a class="btn btn-white" href="{{ path('fos_user_profile_show') }}">Cancel</a>
                            <button class="btn btn-primary" type="submit">{{ 'profile.edit.submit'|trans }}</button>
                        </div>
                    </div>
                </form>
                #}
            </div>
        </div>
    </div>
</div>
